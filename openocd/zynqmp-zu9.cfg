# zynqmp-zu9.cfg — GENERATED by tools/gen-board-cfg.py on 2026-06-11 07:41
# Provenance:
#   adapter : (operator-specified)  ->  interface/ftdi/digilent_jtag_smt2_nc.cfg   (source: --adapter)
#   speed   : 200 kHz
#   target  : target/xilinx_zynqmp.cfg
#   IDCODEs observed in discovery:
#     0x24738093  Xilinx ZynqMP/RFSoC (part 0x4738, rev 2)
#     0x5ba00477  Arm CoreSight DAP (part 0xba00)
#
# CAVEATS — the generator CANNOT verify these; YOU must (see docs/18):
#   * I/O voltage / Vref / level-shifting   (ZynqMP PS-JTAG = 1.8 V)
#   * JTAG pinout / connector
#   * clock-speed stability — 200 kHz is a starting point; raise only if IDCODEs stay clean
#   * transport (jtag vs cJTAG) and SRST/TRST wiring
# This config is VALIDATED ONLY against the board whose discovery log produced it.
# (run from the repo root so repo-relative source paths resolve)

source [find interface/ftdi/digilent_jtag_smt2_nc.cfg]
transport select jtag
adapter speed 200

source [find target/xilinx_zynqmp.cfg]

# APB-debug mem-AP (AP1) for EDPCSR / debug-gate checks (enumerate.tcl §8, jtag-access-check.tcl).
# Must be created at config time; harmless on non-ZynqMP targets.
catch { target create uscale.dbg mem_ap -dap uscale.dap -ap-num 1 }
# probe-board.sh access verdict: OPEN (2026-06-11 07:41) — DAP open, enumeration possible
